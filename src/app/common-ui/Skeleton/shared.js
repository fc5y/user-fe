import { keyframes } from 'styled-components';

export const blinkEffect = keyframes`
  0% { background-color: #e1e4e8; }
  50% { background-color: #edeff2; }
  100% { background-color: #e1e4e8; }
`;
